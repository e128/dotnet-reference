---
name: leverage-advisor-2026-04-11
description: First leverage-advisor run — plans created, script built, key findings
type: project
---

Five plans created on 2026-04-11. Addition winner (violation-scan.sh, 10/12) was built end-to-end in the same run. Omission winner (session-health stats gap, 11/12) remains unbuilt — highest priority for next session.

**Why:** The `session-health.sh stats` subcommand is called by weekly-learner, token-optimizer, and tdd-loop-optimizer but does not exist in the script. All three agents silently degrade when they try to invoke it.

**How to apply:** When next asked to work on session analysis or agent improvements, check the `leverage-missed-session-stats` plan first.

Key subtraction findings (in execution order):
1. `leverage-subtract-simplifier-merge` — merge skill-loop-optimizer into simplification-agent (must run first)
2. `leverage-subtract-commit-paths` — consolidate /commit skill + smart-commit agent
3. `leverage-subtract-gate-reducers` — absorb confirmation-reducer (depends on #1)
