# Markdown Formatting

**Markdown tables must use aligned columns.** Pad every cell with trailing spaces so all columns are the same width. Separator rows use dashes that match their column width exactly.
