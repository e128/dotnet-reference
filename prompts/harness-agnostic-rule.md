add a rule: <describe the rule>

this rule must hold for any coding agent that works in this repo, not just the harness i'm talking to right now. put it in AGENTS.md (or this repo's equivalent portable instruction file), not in a tool-specific file like CLAUDE.md, .cursor/rules, or .claude/rules. if AGENTS.md doesn't exist yet, create it. if the rule only makes sense in terms of one harness's specific mechanics (hooks, skills, subagents, slash commands), say so instead of forcing it in, and propose where it belongs instead.
