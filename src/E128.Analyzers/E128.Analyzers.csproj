<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <IsRoslynComponent>true</IsRoslynComponent>
    <EnforceExtendedAnalyzerRules>true</EnforceExtendedAnalyzerRules>
    <!-- RS2007: the Roslyn 4.14.0 meta-analyzer rejects valid unshipped.md content -->
    <!-- RS1038: intentional — code fix provider requires Workspaces reference in same assembly -->
    <!-- RS0016/RS0017: inherited override members (Initialize, SupportedDiagnostics, etc.) are
         tracked at the type+constructor level only in PublicAPI.Shipped.txt -->
    <NoWarn>$(NoWarn);RS2007;RS1038;RS0016;RS0017;NU5128</NoWarn>
    <!-- PolySharp: netstandard2.0 polyfill for nullable analysis attributes only -->
    <PolySharpIncludeGeneratedTypes>
      System.Diagnostics.CodeAnalysis.NotNullWhenAttribute
    </PolySharpIncludeGeneratedTypes>
  </PropertyGroup>

  <!-- NuGet Packaging -->
  <PropertyGroup>
    <Version>1.32.2</Version>
    <IsPackable>true</IsPackable>
    <IncludeBuildOutput>false</IncludeBuildOutput>
    <DevelopmentDependency>true</DevelopmentDependency>
    <SuppressDependenciesWhenPacking>true</SuppressDependenciesWhenPacking>
    <NoPackageAnalysis>true</NoPackageAnalysis>
    <PackageId>E128.Analyzers</PackageId>
    <Authors>Brent Miller</Authors>
    <Description>Roslyn analyzers enforcing opinionated .NET conventions at compile time. 82 rules with code fixes covering design (sealed-by-default, async void, sync-over-async, ConfigureAwait, in-modifier safety, primary constructor fields, FileSystemInfo equality, TimeProvider, IHttpClientFactory, TimeSpan for durations, mutable collection exposure, DI registration, Task.Run async lambda, excessive inheritance, enum switch, List&lt;T&gt; as IReadOnlyList&lt;T&gt;, Dictionary&lt;K,V&gt; as IReadOnlyDictionary&lt;K,V&gt;, unused interface params, static readonly arrays as ImmutableArray, stale ReferenceAssemblies, readonly struct init, ByteSize for data-size values, ByteSize unwrap via cast), reliability (DateTime.Parse roundtrip, GeneratedRegex timeout/backtracking, hardcoded /tmp paths, Task.FromResult sync I/O, Task.WhenAll unbounded/cancellation, JsonDocument lifetime, concurrency limits, catch filter OCE, options bind init, HttpClient missing OCE catch, FileInfo.Exists TOCTOU, unprotected finally cleanup, disk write-then-read round-trip, pool capacity guard, QuerySelectorAll materialization, TextContent length guard, GetAttribute element type, CompositeDetection specificity), performance (MinBy/MaxBy, HttpCompletionOption, string interpolation, ToArray in foreach, FrozenSet allocation, multi-string OR-chain, static readonly frozen collections, loop complexity, SHA256.HashData, StringBuilderPool), security (FIPS-approved hash algorithms, insecure Random in crypto context), style (string.Empty, Encoding.UTF8, XML doc comments, temp path conventions, null-forgiving operator, suppression audit, pragma balance, IDE1006 naming rule fix), and testing (temp directory cleanup, stale ReferenceAssemblies, Trait category).</Description>
    <PackageTags>roslyn;analyzer;csharp;code-analysis</PackageTags>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <PackageProjectUrl>https://github.com/e128/dotnet-reference</PackageProjectUrl>
    <RepositoryUrl>https://github.com/e128/dotnet-reference</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.CodeAnalysis.CSharp" />
    <PackageReference Include="Microsoft.CodeAnalysis.PublicApiAnalyzers" PrivateAssets="all" />
    <PackageReference Include="Microsoft.CodeAnalysis.Workspaces.Common" />
    <PackageReference Include="PolySharp" PrivateAssets="all" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="E128.Analyzers.Tests" />
  </ItemGroup>

  <ItemGroup>
    <AdditionalFiles Include="AnalyzerReleases.Shipped.md" />
    <AdditionalFiles Include="AnalyzerReleases.Unshipped.md" />
    <AdditionalFiles Include="PublicAPI.Shipped.txt" />
    <AdditionalFiles Include="PublicAPI.Unshipped.txt" />
  </ItemGroup>

  <!-- Place analyzer DLL in the NuGet convention path -->
  <ItemGroup>
    <None Include="$(OutputPath)\$(AssemblyName).dll"
          Pack="true"
          PackagePath="analyzers/dotnet/cs"
          Visible="false" />
  </ItemGroup>

  <!-- Package README -->
  <ItemGroup>
    <None Include="README.md" Pack="true" PackagePath="" />
  </ItemGroup>

</Project>
